<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="container mx-auto py-8">
        <div class="px-10 font-bold">
            <p><a href="/home" class="px-1 underline">HomePage</a>-><a href="/course/<%= course.id %>"
                    class="px-1 underline">
                    <%= course.courseName %>
                </a>-><a href="/chapter/<%= chapter.id %>" class="px-1 underline">
                    <%= chapter.chapterName %>
                </a>-><a href="/newPage/<%= chapter.id %>" class="px-1 underline">Create a new Page</a></p>
        </div>
        <div class="grid grid-cols-6">
            <div class="col-start-3 col-span-2 bg-white border-gray-300 border mr-4 rounded" style="padding: 20px">
                <div class="flex items-center justify-center mb-4">
                    <h2 class="text-2xl font-semibold mb-2 underline">
                        <%= course.courseName %>
                    </h2>
                </div>
                <div class="flex items-center justify-center mb-4">
                    <h2 class="text-2xl font-semibold mb-2 underline">
                        <%= chapter.chapterName %>
                    </h2>
                </div>
                <p class="text-center text-2xl font-bold">Create a new Page</p>
                <form action="/newPage/<%= chapter.id %>" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="py-3">
                        <label for="">Page Title</label>
                        <input type="text" name="pageName" id=""
                            class="border border-gray-300 text-gray-900 rounded p-2 w-full" required>
                    </div>
                    <div class="py-3">
                        <label for="">Content</label>
                        <input type="text" name="pageContent" id=""
                            class="border border-gray-300 text-gray-900 rounded p-2 w-full" required>
                    </div>
                    <div class="py-3">
                        <button type="submit"
                            class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2 mx-auto block">Add
                            Page</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>

</html>